1. The BHSA-Macula Word Level Map is almost complete. 
    a). Only the empty strings [the absorbed "H", definite article] needs to be mapped.
2. The b_lwlc_idy_patch44x.json represents the latest dictionary map of the BHSA word node with the __Leading__ WLC Id from the XML.
3. The bnode_mnode_i.json represents the latest dictionary map of the BHSA word node with the Macula word node(s).
4. the mnode_bnode_i.json is the reverse (Macula is key and BHSA is value) of the above.
    a). This is an unrelaiable map because at least 4 of the Macula keys do not have a corresponding BHSA word node.